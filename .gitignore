# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Builds
dist
dist-ssr
build
*.local

# Environnement (ne jamais committer de secrets)
.env
.env.*
!.env.example

# Documentation interne : reste sur le poste, hors du depot.
# Deja suivis par git avant cette regle, donc retires de l'index avec
# `git rm --cached` — sans quoi la ligne ci-dessous n'aurait aucun effet.
COMPTES-DE-TEST.md
DEPLOIEMENT.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
