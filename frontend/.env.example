# Copier en `.env` et ajuster si besoin.

# Port du serveur de dev Vite
VITE_PORT=5173

# Base URL des appels API. Laisser `/api` pour passer par le proxy Vite en dev.
VITE_API_URL=/api

# Cible du proxy Vite (le backend Express)
VITE_API_PROXY_TARGET=http://localhost:3000
